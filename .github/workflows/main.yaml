on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout file
        uses: actions/checkout@v3
      - name: Start docker 
        env:
          test_secret: ${{ secrets.TEST_SECRET }}
        id: hello  
        uses: Chandolkar001/test_github_actions@9fc5a849723f8b49ed6be24180e6b39cf10d9275
        with:
          who-to-greet: 'vulnerability-scan'


        

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start docker action
        id: hello  
        uses: Chandolkar001/test_github_actions@bd3ca76ed135c2e0b250d7d6daa445bf6b400082
      #   with:
      #     who-to-greet: 'Shreyas'
      # - name: Get the output time
      #   run: echo "The time was ${{ steps.hello.outputs.time }}"
        

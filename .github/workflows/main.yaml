on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout file
        uses: actions/checkout@v3
      - name: Start docker 
        id: hello  
        uses: Chandolkar001/test_github_actions@d01a37c95071aedbb41cfa0908687854c0834676
        with:
          who-to-greet: 'vulnerability-scan'
          secret-val: ${{ secrets.TEST_SECRET }}

        

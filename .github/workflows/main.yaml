on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start docker action
        id: hello  
        uses: Chandolkar001/test_github_actions@19a418c7979a82f12f169403ac8240280707e2a6
        with:
          who-to-greet: 'Shreyas'
      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.time }}"
        

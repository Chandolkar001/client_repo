on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start docker action
        id: hello  
        uses: Chandolkar001/test_github_actions@97d0fe83540794c1f433090a393ccb54e2e8818a
      #   with:
      #     who-to-greet: 'Shreyas'
      # - name: Get the output time
      #   run: echo "The time was ${{ steps.hello.outputs.time }}"
        

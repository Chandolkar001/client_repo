on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout file
        uses: actions/checkout@v3
      - name: Start docker action
        id: hello  
        uses: Chandolkar001/test_github_actions@82897de6db86323404d330ea769db70e4571a2ed
        with:
          who-to-greet: 'secret scan'

        

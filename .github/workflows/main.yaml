on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start docker
        id: hello  
        uses: Chandolkar001/test_github_actions@1509da0c20f1d7b1304c60f75fd6c14e4e974515
      #   with:
      #     who-to-greet: 'Shreyas'
      # - name: Get the output time
      #   run: echo "The time was ${{ steps.hello.outputs.time }}"
        

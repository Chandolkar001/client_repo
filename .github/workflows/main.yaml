on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start docker action
        id: hello  
        uses: Chandolkar001/test_github_actions@e28584bed2f379fea4b34ddcb4cd0198c152f1f4
      #   with:
      #     who-to-greet: 'Shreyas'
      # - name: Get the output time
      #   run: echo "The time was ${{ steps.hello.outputs.time }}"
        

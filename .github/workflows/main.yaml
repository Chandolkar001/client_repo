on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout file
        uses: actions/checkout@v3
      - name: Start docker 
        env:
          test_secret: ${{ secrets.TEST_SECRET }}
        id: hello  
        uses: Chandolkar001/test_github_actions@b34a2240adab7928c46ea8ae5f8bd36c0f0de6da
        with:
          who-to-greet: 'vulnerability-scan'
          secret-val: test_secret


        

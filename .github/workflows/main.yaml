on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout file
        uses: actions/checkout@v3
      - name: Start docker 
        env:
          test_secret: ${{ secrets.TEST_SECRET }}
        id: hello  
        uses: Chandolkar001/test_github_actions@cc4fc83877fde765437d833111c2061682129acc
        with:
          who-to-greet: 'vulnerability-scan'


        
